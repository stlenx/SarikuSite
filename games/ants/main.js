let WORLD_SIZE = 27;

let map = new Array(WORLD_SIZE);
for(let i = 0; i < map.length; i++) {
    map[i] = new Array(WORLD_SIZE);
}

function setup() {
    for(let x = 0; x < map.length; x++) {
        for(let y = 0; y < map.length; y++) {

        }
    }
}

console.log(map);